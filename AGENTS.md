# SolarWorkflows Agent Rules

- This is a multi-tenant SaaS app.
- Never hardcode company-specific values.
- Every business table must use `company_id`.
- UI must be mobile-first.
- Use Supabase for backend, authentication, and storage.
- Use TypeScript.
- Keep code modular by module folder.
- Do not build business logic yet.
