import { Stack } from "expo-router";
import { SafeAreaView } from "react-native-safe-area-context";
import { ScrollView, StyleSheet, Text, View } from "react-native";
import { colors } from "@/theme";

const groups = [{ title: "Overdue", items: ["Collect project payment", "Follow up on pending quotation"] }, { title: "Due today", items: ["Call back new enquiries", "Confirm site survey", "Review low-stock items"] }];
export default function DueActionsScreen() { return <SafeAreaView style={styles.safe}><Stack.Screen options={{ headerShown: true, title: "Due actions" }} /><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Due actions</Text><Text style={styles.copy}>Items that need your attention today.</Text>{groups.map((group) => <View key={group.title} style={styles.group}><Text style={styles.groupTitle}>{group.title}</Text>{group.items.map((item) => <View key={item} style={styles.row}><View style={[styles.dot, group.title === "Overdue" && styles.danger]} /><Text style={styles.label}>{item}</Text></View>)}</View>)}</ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { padding: 18 }, title: { color: colors.navy, fontSize: 28, fontWeight: "900" }, copy: { color: colors.muted, marginTop: 5, marginBottom: 22 }, group: { backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border, borderRadius: 17, padding: 16, marginBottom: 14 }, groupTitle: { color: colors.navy, fontSize: 16, fontWeight: "900", marginBottom: 8 }, row: { minHeight: 48, flexDirection: "row", alignItems: "center", borderTopWidth: StyleSheet.hairlineWidth, borderTopColor: colors.border }, dot: { width: 8, height: 8, borderRadius: 4, backgroundColor: colors.orange, marginRight: 11 }, danger: { backgroundColor: colors.danger }, label: { color: colors.ink, fontWeight: "700" } });
