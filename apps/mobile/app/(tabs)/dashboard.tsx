import { ActivityIndicator, RefreshControl, ScrollView, StyleSheet, Text, View } from "react-native";
import { useQuery } from "@tanstack/react-query";
import { mobileApi } from "@/lib/api";
export default function DashboardScreen() { const query = useQuery({ queryKey: ["dashboard"], queryFn: mobileApi.dashboard }); if (query.isLoading) return <ActivityIndicator style={{ flex: 1 }} />; const data = query.data?.data; return <ScrollView style={styles.page} contentContainerStyle={styles.content} refreshControl={<RefreshControl refreshing={query.isFetching} onRefresh={() => void query.refetch()} />}><Text style={styles.eyebrow}>BIZLEE</Text><Text style={styles.title}>Business overview</Text>{query.error instanceof Error && <Text style={styles.error}>{query.error.message}</Text>}<View style={styles.grid}><Metric label="Open enquiries" value={data?.openEnquiries ?? 0} /><Metric label="Active projects" value={data?.activeProjects ?? 0} /><Metric label="Due follow-ups" value={data?.dueFollowups ?? 0} /></View><Text style={styles.updated}>Updated {query.data?.meta.fetchedAt ? new Date(query.data.meta.fetchedAt).toLocaleTimeString() : "—"}</Text></ScrollView>; }
function Metric({ label, value }: { label: string; value: number }) { return <View style={styles.card}><Text style={styles.value}>{value}</Text><Text style={styles.label}>{label}</Text></View>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: "#f8fafc" }, content: { padding: 20, paddingTop: 62 }, eyebrow: { color: "#f97316", fontWeight: "900", letterSpacing: 2 }, title: { fontSize: 30, color: "#06173f", fontWeight: "800", marginTop: 6, marginBottom: 18 }, grid: { gap: 12 }, card: { backgroundColor: "white", padding: 20, borderRadius: 16, borderWidth: 1, borderColor: "#e2e8f0" }, value: { fontSize: 32, fontWeight: "900", color: "#0f766e" }, label: { color: "#475569", marginTop: 4 }, updated: { color: "#94a3b8", marginTop: 16 }, error: { color: "#b91c1c", marginBottom: 12 } });
