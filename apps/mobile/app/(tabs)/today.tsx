import { ActivityIndicator, RefreshControl, ScrollView, StyleSheet, Text, View } from "react-native";
import { useQuery } from "@tanstack/react-query";
import { mobileApi } from "@/lib/api";
export default function TodayScreen() {
  const query = useQuery({ queryKey: ["daily-brief"], queryFn: mobileApi.brief });
  return <ScrollView style={styles.page} contentContainerStyle={styles.content} refreshControl={<RefreshControl tintColor="white" refreshing={query.isFetching} onRefresh={() => void query.refetch()} />}><Text style={styles.eyebrow}>BIZLEE AI</Text><Text style={styles.title}>{query.data?.brief.headline ?? "Your daily brief"}</Text>{query.isLoading && <ActivityIndicator color="white" />}{query.error instanceof Error && <View style={styles.card}><Text style={styles.cardTitle}>Brief unavailable</Text><Text style={styles.body}>{query.error.message}</Text></View>}{query.data?.brief.cards.map((card, index) => <View key={`${card.title}-${index}`} style={[styles.card, card.severity === "critical" && styles.critical]}><Text style={styles.severity}>{card.severity}</Text><Text style={styles.cardTitle}>{card.title}</Text><Text style={styles.body}>{card.body}</Text></View>)}</ScrollView>;
}
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: "#06173f" }, content: { padding: 20, paddingTop: 62, paddingBottom: 40, gap: 14 }, eyebrow: { color: "#fb923c", fontWeight: "900", letterSpacing: 2 }, title: { color: "white", fontSize: 30, fontWeight: "800", marginBottom: 4 }, card: { backgroundColor: "#ffffff12", borderColor: "#ffffff22", borderWidth: 1, borderRadius: 18, padding: 20 }, critical: { borderColor: "#fb7185" }, severity: { color: "#fdba74", textTransform: "uppercase", fontSize: 11, fontWeight: "900", marginBottom: 7 }, cardTitle: { color: "white", fontWeight: "800", fontSize: 18, marginBottom: 8 }, body: { color: "#dbeafe", lineHeight: 22 } });
